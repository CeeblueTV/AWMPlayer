{
  "name": "awm-player",
  "version": "1.0.2",
  "description": "AwmPlayer",
  "main": "dist/player.js",
  "scripts": {
    "build": "./build.sh $npm_package_version $(date +%Y-%m-%d-%H:%M:%S)",
    "lint": "eslint ./dist/player.js",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "clean-css-cli": "^4.2.1",
    "terser": "^3.17.0"
  },
  "devDependencies": {
    "eslint": "^7.28.0"
  }
}
